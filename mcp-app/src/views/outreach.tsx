export default function Outreach({ toolOutput }: { toolOutput?: { lead?: { business_name?: string }; channel?: string; message?: string; approved_for_sending?: boolean } }) {
  return <div style={{ fontFamily: "Inter, system-ui, sans-serif", padding: 20, maxWidth: 760, margin: "auto" }}><div style={{ fontSize: 12, opacity: .6 }}>AUTOMIQ ⚡ · OUTREACH</div><h2 style={{ margin: "5px 0 4px" }}>{toolOutput?.lead?.business_name ?? "Personalized message"}</h2><div style={{ fontSize: 13, opacity: .6, marginBottom: 14 }}>{toolOutput?.channel ?? "Channel"} · Approval required before sending</div><div style={{ border: "1px solid #e5e7eb", borderRadius: 14, padding: 16, whiteSpace: "pre-wrap", lineHeight: 1.55 }}>{toolOutput?.message ?? "Ask the assistant to generate outreach for a lead."}</div><div style={{ marginTop: 12, fontSize: 12, opacity: .55 }}>Sending is disabled in this MVP. The draft must be explicitly approved before any future sending integration can execute.</div></div>;
}
